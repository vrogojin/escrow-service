/**
 * Address utilities — re-exported from sphere-sdk (single source of truth).
 */
export { parseAddress, isValidAddress, isValidDirectAddress, normalizeAddress, addressesMatch } from '@unicitylabs/sphere-sdk';
export type { AddressType, ParsedAddress } from '@unicitylabs/sphere-sdk';
